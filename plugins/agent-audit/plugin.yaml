name: agent-audit
version: 0.5.0
description: Privacy-conscious Agent discovery and verification audit log with project-configured rule mapping.
kind: standalone
provides_hooks:
  - on_skill_lifecycle
  - post_tool_call
  - pre_verify
  - on_session_end
